spring:
  application:
    name: prov-dashboard
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9099/eureka
    register-with-eureka: true
    fetch-registry: true 
  instance:    hostname: localhost  
       
    
    
